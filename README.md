# CRWN CLOTHING

## An Ecommerce website Built with React Ecosystem technologies.

#### Packages Used :

-   `react` ->
-   `react-router-dom` ->
-   `redux` ->
-   `react-redux` ->
-   `redux-logger` ->
-   `reselect` ->
-   `firebase` ->
-   `node-sass` ->
-   `redux-persist` ->
